# mood-cloud-config
* mood.yml
* mood-dev.yml
* mood-prod.yml

##mood.yml
for real, a file. set real server


##mood-dev.yml
for dev, a file. set dev


##mood-prod.yml
for prod, a file. set test and prod
